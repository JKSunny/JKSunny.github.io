services:
  php:
    image: ghcr.io/jksunny/jksunny.github.io:latest
    container_name: jka_vk_metrics_backend
    ports:
      - "9090:8080"
    restart: unless-stopped
    # environment:
    #   GITHUB_TOKEN: ${GITHUB_TOKEN}